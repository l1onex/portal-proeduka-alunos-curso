/**
 * Antes: a ficha admin exigia marcar "Conferi esta informação" em cada campo
 * (state `fieldReview`) antes do botão "Iniciar certificado" ficar disponível.
 *
 * Agora: a aprovação é direta — basta o aluno não ter pendências (campos
 * preenchidos + toda a documentação enviada). Este ficheiro fica vazio de
 * propósito, para que imports antigos não quebrem.
 */
export {};
